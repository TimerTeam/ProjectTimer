{% block body %}

   <link href="{{ asset('css/timer.css') }}" type="text/css" rel="stylesheet">


    <nav class="navbar navbar-expand-lg ">
        
            <div class="collapse navbar-collapse menu">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a href="{{ path('home') }}" class="host">Accueil</a>
                    </li>

                </ul>

                <ul class="navbar-nav">

                    <li>
                        <a href="{{ path('user_list') }}" class="host">Liste des utilisateurs</a>
                    </li>
                </ul>

                   <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                            {% if is_granted('ROLE_USER') %}
                                <a href="{{ path('profile') }}" class="btn btn-outline-info">Mon Profil</a>
                            {% endif %}
                        {% else %}
                            <a href="{{ path('user-create') }}" class="btn btn-outline-warning account">Inscription</a>
                        {% endif %}
                    </li>
                </ul>

                <ul class="navbar-nav" >

                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item">
                        <a href="{{ path('app_logout') }}" class="btn btn-outline-danger">Déconnexion</a>
                        {% else %}
                            <a href="{{ path('app_login') }}" class="btn btn-outline-success account">Connexion</a>
                        {% endif %}
                    </li>

                </ul>
                
             

            </div>

    </nav>
{% endblock %}
